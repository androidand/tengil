description: macOS Time Machine backup target with Apple-specific SMB extensions

timemachine:
  profile: backups
  consumers:
    - type: smb
      name: "Time Machine"
      access: write
      options:
        fruit: true
        timemachine: true
